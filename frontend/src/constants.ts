// App-wide domain constants. Cross-cutting values that aren't tied to a single
// component or feature live here (component-scoped constants stay colocated).

/** GitHub host used to recognize and normalize repo URLs / `owner/name` input. */
export const GITHUB_HOST = 'github.com';
