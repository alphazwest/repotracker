# Frontend environment — copy to .env and adjust if needed.

# GraphQL endpoint the Apollo Client connects to (wired in ticket 007).
# Matches the backend default port.
VITE_GRAPHQL_URL="http://localhost:4000/"
